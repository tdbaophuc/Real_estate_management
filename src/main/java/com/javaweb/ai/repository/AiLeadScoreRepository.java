package com.javaweb.ai.repository;

import com.javaweb.ai.entity.AiLeadScore;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiLeadScoreRepository extends JpaRepository<AiLeadScore, Long> {
}
